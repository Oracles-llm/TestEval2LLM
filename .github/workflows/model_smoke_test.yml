name: Model Smoke Test

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  llama-cpp-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache model files
        uses: actions/cache@v4
        with:
          path: |
            TestEval2LLM/models
            TestEval2LLM/gguf
          key: ${{ runner.os }}-model-${{ hashFiles('TestEval2LLM/requirements.txt', 'TestEval2LLM/config.py') }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r TestEval2LLM/requirements.txt
          pip install -r SystemEvaluation/requirements.txt

      - name: Download model
        run: python download_model.py
        working-directory: TestEval2LLM

      - name: Convert to GGUF
        run: python make_gguf.py
        working-directory: TestEval2LLM

      - name: Run prompt suite
        run: python run_prompts.py
        working-directory: TestEval2LLM

      - name: Evaluate responses with SystemEvaluation
        run: python run_eval_responses.py
        working-directory: SystemEvaluation

      - name: Upload responses
        uses: actions/upload-artifact@v4
        with:
          name: model-responses
          path: TestEval2LLM/results/responses.jsonl

      - name: Upload evaluation report
        uses: actions/upload-artifact@v4
        with:
          name: evaluation-report
          path: SystemEvaluation/results/*.json
