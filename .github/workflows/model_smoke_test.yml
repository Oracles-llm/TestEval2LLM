name: Model Smoke Test

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  llama-cpp-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache model files
        uses: actions/cache@v4
        with:
          path: |
            models
            gguf
          key: ${{ runner.os }}-model-${{ hashFiles('requirements.txt', 'config.py') }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Download model
        run: python download_model.py

      - name: Convert to GGUF
        run: python make_gguf.py

      - name: Run dataset suite
        run: python run_dataset_suite.py

      - name: Upload responses
        uses: actions/upload-artifact@v4
        with:
          name: model-responses
          path: results/responses.jsonl
      
      - name: Trigger SystemEvaluation workflow
        env:
          SYSTEMEVAL_TOKEN: ${{ secrets.SYSTEMEVAL_TOKEN }}
          SYSTEMEVAL_REPO: Oracles-llm/SystemEvaluation
        run: |
          curl -X POST \
            -H "Authorization: Bearer $SYSTEMEVAL_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${SYSTEMEVAL_REPO}/dispatches" \
            -d "{\"event_type\":\"teval_complete\",\"client_payload\":{\"source_repo\":\"${{ github.repository }}\",\"run_id\":\"${{ github.run_id }}\"}}"
